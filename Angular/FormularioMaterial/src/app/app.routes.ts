import { Routes } from '@angular/router';
import { FormularioPersonaComponent } from './formulario-persona/formulario-persona.component';

export const routes: Routes = [{ path: 'formulario', component: FormularioPersonaComponent }];
